[source]
git = "https://gitlab.redox-os.org/redox-os/extrautils.git"
rev = "97bf8c6b6eade670e98de18a04a6f1b08ebcaf84"

[build]
template = "custom"
dependencies = [
    "xz"
]
script = """
# TODO: Can't be linked correctly yet
# DYNAMIC_INIT

if [ "${COOKBOOK_DYNAMIC}" != "1" ]; then
    install_flags+=" --features=static"
fi

cookbook_cargo
"""
