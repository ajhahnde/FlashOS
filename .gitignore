# macOS clutter
.DS_Store

# Rust build artefacts (Cargo.lock is tracked: the toolchain is pinned)
target/
rust-out/

# Private notes, management, and local session state
/ajhahnde/
/management/

# Local secrets and signing material (versions.env is intentionally public)
.env
.env.*
*.env
!versions.env
*.key
*.pem
*.p12
*.pfx

# Python bytecode cache (boot-demo replay script)
/scripts/__pycache__/

# Logs, serial captures, and hardware evidence
*.log
*.raw
*.capture
screenlog.*

# tour web app — keep source, drop deps & scratch
/tour/node_modules/
/tour/temp/
/tour/SETUP.md
